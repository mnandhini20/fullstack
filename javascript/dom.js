var division = document.querySelector("#select");

// Use firstChild to get the first child node of the selected element
var get = division.firstChild;

// Log the result to the console
console.log(get);
